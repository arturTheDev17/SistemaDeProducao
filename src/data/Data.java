package data;

/**
 * Interface representing a data structure.
 * This interface serves as a marker for various data classes in the system,
 * allowing for a unified type for different data implementations.

 * Classes implementing this interface should define specific data behaviors
 * and attributes relevant to their context.
 */
public interface Data {
    // This interface can be extended in the future to include common methods
    // for data classes, if needed.
}
